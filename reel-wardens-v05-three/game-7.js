function renderOver(){return`<div class="overlay"><div class="sheet"><div class="eyebrow">RUN COMPLETE</div><h1>${st.base>0?'守线成功':'防线崩溃'}</h1><p>最终分数 ${st.score} · 本机最高 ${st.high} · 构筑 ${st.rewards.length} 项。</p><button class="big-cta" data-action="restart">再来一局</button></div></div>`}
function bind(){$$('[data-action]').forEach(el=>el.addEventListener('click',()=>{const a=el.dataset.action;if(a==='start')start();else if(a==='spin')spin();else if(a==='arm')arm();else if(a==='battle')battle();else if(a==='three'){if(window.RW3D&&window.RW3D.ready)window.RW3D.toggle();}else if(a==='speed'){st.battleSpeed=st.battleSpeed===2?1:2;render();}else if(a==='unit')selectUnit(el.dataset.id);else if(a==='reward')chooseReward(el.dataset.id);else if(a==='evo')chooseEvo(el.dataset.id);else if(a==='restart')restart()}))}
function render(){const app=$('#app');if(st.screen==='menu'){app.innerHTML=renderMenu();bind();return}app.innerHTML=renderGame()+(st.screen==='reward'?renderRewardOverlay():'')+(st.evo?renderEvoOverlay():'')+(st.screen==='over'?renderOver():'');bind()}
render();
